def search_web(query: str) -> str:
    """
    Simulates a web search.

    query: the search query string
    returns: fake search results as text
    """

    return f"Simulated search results for: {query}"

